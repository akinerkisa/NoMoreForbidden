# Support

Use GitHub Issues for reproducible bugs and feature requests. Include:

- operating system and Python version;
- installation method and exact command;
- sanitized target shape or local lab route;
- output format and relevant error text;
- whether the issue reproduces with `--dry-run`.

Never include credentials, tokens, private URLs, or live security findings.
For undisclosed vulnerabilities, follow SECURITY.md instead.
